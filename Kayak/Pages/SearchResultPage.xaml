<?xml version="1.0" encoding="UTF-8"?>
<kayak:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:kayak="clr-namespace:Kayak;assembly=Kayak" xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Class="Kayak.SearchResultPage">
	<kayak:BasePage.Content>
		<StackLayout Padding="20" Spacing="20" VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand">
			<Label Text="{Binding CurrentDateTime}" />
			<Label Text="{Binding TripType}" />
			<Label Text="{Binding CityFrom}" />
			<Label Text="{Binding CityTo}" />
			<Label Text="{Binding SelectedDateRange}" />
		</StackLayout>
	</kayak:BasePage.Content>
</kayak:BasePage>